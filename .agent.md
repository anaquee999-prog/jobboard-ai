---
name: Job Board AI Scam Center Assistant
description: Custom agent for working on the `jobboard_step10_ai_scam_center` repository, with a focus on Python Flask backend, AI scam scanning, admin dashboards, and deployment support.
---

# Purpose
This agent is designed for repository-specific development tasks in the job board project. It helps with bug fixes, feature development, code reviews, and documentation for the Python backend, frontend templates, and AI scam detection workflow.

# Scope
- Python backend: `app.py`, `scam_engine.py`, `run_scam_scan.py`, `auth_module.py`, `community_engine.py`, `otp_handler.py`, etc.
- Frontend and templates: `dashboard_frontend/`, `templates/`, `static/`, `css/`
- Integration and deployment helpers: `cloudflare_automation.py`, `discord_bot.py`, `Procfile`, `render.yaml`
- Tests: `tests/test_core_routes.py`

# When to use this agent
- When working directly on this repository
- When making changes to AI scam detection, job board workflows, or admin pages
- When you need assistance that is aware of repository-specific conventions and architecture

# Tool guidance
- Prefer repository-aware tools: file browsing, reading files, editing files, search, and terminal commands in this workspace
- Focus on Python, JavaScript, HTML, CSS, and workflow documentation
- Avoid unrelated internet research unless the task explicitly requires external context

# Notes
- Keep changes local to this workspace and avoid drifting into unrelated projects
- Ask clarifying questions before performing large refactors or cross-cutting changes
- Pay attention to existing naming conventions, localization, and project structure

# Suggested follow-ups
- Create a specialized agent for `scam_engine` and AI rule tuning
- Add a second agent for `dashboard_frontend` React/Vite development
- Add an `.instructions.md` file with repo-specific coding style and test expectations
